{% extends 'base.html.twig' %}

{% block title %}Auteur {% endblock %}

{% block body %}
    <div>
     <a href="{{ path('app_auteur_new') }}"><span class="btn btn-primary"><i class="fa fa-plus">&nbsp;</i>Nouveau</span></a>
</div><br>

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prenom</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for auteur in auteurs %}
            <tr>
                <td>{{ auteur.id }}</td>
                <td>{{ auteur.prenom }}</td>
                <td>{{ auteur.nom }}</td>
               <td>
  <a href="{{ path('app_auteur_show', {'id': auteur.id}) }}"><span class="btn btn-default">   <i class="fa fa-eye"></i></span>
  
  </a>

  <a href="{{ path('app_auteur_edit', {'id': auteur.id}) }}" ><span class="btn btn-success"> <i class="fa fa-edit"></i></span>
    
   
  </a>

  <a href="{{ path('app_auteur_delete', {'id': auteur.id}) }}"> <span class="btn btn-danger"><i class="fa fa-trash-o"></i></span>
    

  </a>
</td>


            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   {% endblock %}
