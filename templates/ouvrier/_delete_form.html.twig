<form method="post" action="{{ path('app_ouvrier_delete', {'id': ouvrier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ouvrier.id) }}">
    <button class="btn">Delete</button>
</form>
